version: 2

models: 
  - name: dim_customers
    columns: 
      - name: customer_id
        description: primary key constraints
        tests:
          - unique
          - not_null
  - name: fct_orders
    columns: 
      - name: order_id
        description: primary key constraints
        tests:
          - unique
          - not_null